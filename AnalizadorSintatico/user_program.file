program teste4
declare
integer divisor, number, task, teste, blah;
boolean bowling, racing;
integer nofactor, prime;
do read(number);
write(number);
divisor := number;
nofactor := true;
if (number) then nofactor := false
else divisor := divisor - 1.1
end
prime := nofactor;
if prime then write('S') else write('N') end
end
